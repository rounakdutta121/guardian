<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SHORT_SERVICE" />

    <application>
        <receiver
            android:name=".FakeCallAlarmReceiver"
            android:exported="false" />
        <receiver
            android:name=".CheckinExpireReceiver"
            android:exported="false"
            android:enabled="true" />
        <receiver
            android:name=".CheckinCallReceiver"
            android:exported="false"
            android:enabled="true" />
        <service
            android:name=".CheckinEscalationService"
            android:exported="false"
            android:foregroundServiceType="dataSync|shortService" />
        <service
            android:name=".CheckinCallService"
            android:exported="false"
            android:foregroundServiceType="dataSync|shortService" />
    </application>
</manifest>
