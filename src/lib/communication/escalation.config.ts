/** Delay between SMS sends to each contact in the escalation chain. */
export const ESCALATION_SMS_DELAY_MS = 2_000;

/** Wait after placing a call before trying the next contact (no answer window). */
export const ESCALATION_CALL_WAIT_MS = 35_000;
